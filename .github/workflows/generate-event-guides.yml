name: Generate Event Guides

on:
  workflow_dispatch:

jobs:
  generate-event-guides:
    if: ${{ false }} # Disabled: prevent any runs until re-enabled.
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE: ${{ secrets.SUPABASE_SERVICE_ROLE }}
      SUPABASE_MEDIA_BUCKET: ${{ secrets.SUPABASE_MEDIA_BUCKET }}
      SITE_URL: ${{ secrets.SITE_URL }}
      ARTICLE_AUTHOR_ID: ${{ secrets.ARTICLE_AUTHOR_ID }}
      ROBLOX_VIRTUAL_EVENTS_LIMIT: "200"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Refresh virtual events
        run: npm run collect:virtual-events

      - name: Queue event guides
        run: npm run queue:event-guides

      - name: Generate event guide
        run: npm run generate:events-articles
